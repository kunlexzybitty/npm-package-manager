{
  "name": "minor-patch-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Scan Node/React projects for minor & patch updates, compare security risk, and apply upgrades from a clean web UI.",
  "type": "module",
  "bin": {
    "mpm": "server/index.js"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "semver": "^7.6.3"
  }
}
